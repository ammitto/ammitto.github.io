node_modules
tmp
artifacts
dist
_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
.DS_Store
Gemfile.lock

# TypeScript build cache
*.tsbuildinfo

# Vite SSG temp directory
.vite-ssg-temp

# Type-erased modules emitted for the unit tests (tsconfig.test.json)
.test-build

# Playwright output (traces, screenshots and the HTML report from test:e2e)
test-results
playwright-report